node_modules

# Secrets — never commit
config.json
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Build artifacts
dist/

# Local runtime data (fetched from R2 at runtime; session state)
data/

# Editor local
*.code-workspace.local
scripts/.bak/
